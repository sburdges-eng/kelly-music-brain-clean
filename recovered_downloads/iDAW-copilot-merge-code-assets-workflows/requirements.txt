# iDAW Combined Requirements
# This file combines requirements from both penta-core and DAiW-Music-Brain

# Core Python dependencies
numpy>=1.21.0         # Numerical operations (used by both)
mido>=1.2.10          # MIDI file I/O (DAiW-Music-Brain)

# Optional audio analysis (install with: pip install -e ".[audio]")
# librosa>=0.9.0      # Audio analysis
# soundfile>=0.10.0   # Audio file I/O

# Optional music theory (install with: pip install -e ".[theory]")
# music21>=7.0.0      # Advanced music theory

# Desktop UI dependencies (install with: pip install -e ".[ui]")
# streamlit>=1.28.0   # Web UI framework

# Native desktop app (install with: pip install -e ".[desktop]")
# streamlit>=1.28.0   # Web UI framework
# pywebview>=4.0.0    # Native window wrapper

# Build tools for standalone executable (install with: pip install -e ".[build]")
# pyinstaller>=6.0.0  # Executable packaging

# Development dependencies (install with: pip install -e ".[dev]")
# pytest>=7.0.0       # Testing framework
# black>=22.0.0       # Code formatter
# flake8>=4.0.0       # Linter

# C++ Build Requirements (for penta-core)
# - CMake 3.15+
# - C++17 compiler (GCC 9+, Clang 10+, MSVC 2019+)
# - JUCE framework (for plugin build)
# - pybind11 (for Python bindings)

# Note: The original requirements files are preserved as:
# - requirements_penta-core.txt
# - requirements_music-brain.txt
